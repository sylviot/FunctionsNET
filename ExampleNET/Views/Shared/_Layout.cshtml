<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    @if (!Request.RequestContext.RouteData.GetRequiredString("action").Equals("Index", StringComparison.InvariantCultureIgnoreCase) &
         !Request.RequestContext.RouteData.GetRequiredString("controller").Equals("Home", StringComparison.InvariantCultureIgnoreCase))
    { <a href="@Url.Action("Index","Home")">< back</a> }
     

    @RenderBody()
</body>
</html>